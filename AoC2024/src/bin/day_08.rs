mod utils;

use utils::input::read_lines;

use std::collections::HashMap;

use std::time::Instant;

fn main() {
    let start = Instant::now();
    let input = read_lines("./src/bin/inputs/day_08_test.txt");

    let part_1_ans: usize;
}